# Installation

Install using yarn:

````
yarn add esp-js
```
usage:

``` js
import * as esp from 'esp-js';
var router = new esp.Router();

// or
import { Router } from 'esp-js';
var router = new Router();
 
```