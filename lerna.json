{
  "version": "2.0.1-rc.1",
  "useWorkspaces": true,
  "npmClient": "yarn",
  "command": {
    "publish": {
      "allowBranch": "*",
      "ignoreChanges": [
        "**/test/**",
        "**/*.md"
      ],
      "forcePublish": true,
      "bump": "prerelease",
      "preid": "rc",
      "dist-tag": "next",
      "npmClient": "yarn",
      "message": "chore(release): publish %s"
    },
    "version": "fixed"
  },
  "packages": [
    "packages/*",
    "examples/*"
  ]
}
