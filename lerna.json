{
  "version": "6.2.1",
  "useWorkspaces": true,
  "npmClient": "yarn",
  "command": {
    "publish": {
      "allowBranch": "*",
      "ignoreChanges": [
        "**/test/**",
        "**/*.md"
      ],
      "forcePublish": true,
      "npmClient": "yarn",
      "message": "chore(release): publish %s"
    },
    "version": "fixed"
  },
  "packages": [
    "packages/*",
    "examples/*",
    "docs/*"
  ]
}
